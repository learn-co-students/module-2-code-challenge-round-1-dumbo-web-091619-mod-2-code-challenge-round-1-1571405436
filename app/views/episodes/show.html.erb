<h1><%= "Episode #{@episode.number}"%></h1>

<h1>List of All Guests:</h1>
<ul>
  <% @guests.each do |guest| %>
  <li><%= link_to guest.name, guest_path(guest) %></li>
  <% end %>
</ul>

<form>
  <input type="button" onclick="window.location.href = '<%= episodes_path %>';" value="Back To Episodes"/>
</form>
